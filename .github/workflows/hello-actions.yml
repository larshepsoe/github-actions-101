name: "Hello Actions!"

on:
  workflow_dispatch:
  # push:


jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: "Echo greeting"
        run: "echo 'hei på deg'"
      - name: "Echo goodnight"
        run: "echo 'hadet'"
